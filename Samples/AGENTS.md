# Sample Script Instructions

These `.cs` files are sample .NET file-based apps and must remain runnable as-is with `dotnet run <file>.cs -- [arguments]`. Keep each sample self-contained; do not require a `.csproj`, generated files, or manual setup unless the sample documents it.

There is a nested instruction for [worktree-path-selector installation](worktree-path-selector/instructions.md) for working with the worktree-path-selector 
